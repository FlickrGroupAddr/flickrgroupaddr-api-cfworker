export {};

declare global {
    const FGA_FLICKR_OAUTH_REQUEST_TOKEN_VERB: string;
    const FGA_FLICKR_OAUTH_REQUEST_TOKEN_ENDPOINT_URI: string;
    const FGA_FLICKR_OAUTH_CALLBACK_URI: string;
    const FGA_FLICKR_OAUTH_APP_KEY: string;
}
